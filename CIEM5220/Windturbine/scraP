# Do not copy paste.
# this plot shows where wave energy is concentrated, where the structure is sensitive, where the product of the two is larget (the dangerous overlap)

fig, ax1 = plt.subplots(figsize=(10, 5))

ax1.plot(omega_axis, S_jonswap_LC1, 'b', label='JONSWAP LC1')
ax1.plot(omega_axis, S_jonswap_LC2, 'orange', label='JONSWAP LC2')
ax1.set_xlabel('Frequency [rad/s]')
ax1.set_ylabel('Wave energy S(ω) [m²s/rad]')

ax2 = ax1.twinx()      # second y-axis
# placeholder for transfer function - we'll add this in step 5
# ax2.plot(omega_axis, H_moment, 'k--', label='|H(ω)|²')
ax2.set_ylabel('Structural response |H(ω)|²')

ax1.axvline(2*np.pi*f1, color='gray', linestyle='--', linewidth=1.5, label='f1')
ax1.axvline(2*np.pi*f2, color='gray', linestyle=':', linewidth=1, label='f2')
ax1.legend()
ax1.grid(True)
plt.title('Wave energy vs structural frequency')
plt.tight_layout()
plt.show()
